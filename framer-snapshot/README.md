This folder contains a snapshot of the Framer project structure pulled via the `unframer` MCP (XML for pages + CMS schema).

If you later enable React Export in Unframer, we can generate a full 1:1 React code export and commit that too.

